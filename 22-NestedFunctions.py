# Nested Functions calls = function calls inside another function
#                   innermost function calls are executed first
#                   returns value is used as argument for the next outer function

print(round(abs(float(input("Enter a whole positive number:")))))

